apiVersion: v1
kind: Service
metadata:
  name: sample-nodeport-fail2
spec:
  type: NodePort
  selector:
    app: sample-app
  ports:
    - name: "http-port"
      protocol: "TCP"
      port: 8080
      targetPort: 80
      nodePort: 30080 # 이전의 sample-nodeport와 포트 중복
